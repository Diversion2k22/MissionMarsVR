using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeltSettings : MonoBehaviour
{
    //alpha = alpha - (int)tau*(alpha/tau);
        

        // if(rotationClockwise)
        // {
        //     transform.RotateAround(parent.transform.position, parent.transform.up, orbitSpeed * Time.deltaTime);
        // }
        // else
        // {
        //     transform.RotateAround(parent.transform.position, -parent.transform.up, orbitSpeed * Time.deltaTime);
        // }

















        // x = randomRadius*Mathf.Cos(randomRadian);
                // z = randomRadius*Mathf.Sin(randomRadian);
                // randomRadian = Random.Range(-Mathf.PI, Mathf.PI);
                // tanValue=Mathf.Tan(randomRadian);

                
                // x=(useA*useB)/Mathf.Sqrt(useB*useB + useA*useA*tanValue*tanValue);
                // q=x/useA;
                // z=Mathf.Sqrt(1-q*q)*useB;
                //alpha = (rr*Time.time + Time.time * rotationSpeed/100) % tau;
}